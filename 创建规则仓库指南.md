# 创建规则仓库和配置双向同步指南

## 🎯 目标

创建一个新的GitHub仓库 `stm32-cursor-rules` 来专门保存和管理Cursor AI规则，并实现与项目仓库的双向同步。

---

## ✅ 已完成的工作

### 1. 规则整合 ✅
- ✅ 添加元规则系统（最高优先级）
- ✅ 添加强制检查点系统（4个检查点）
- ✅ 添加完整验证流程规范（STM32版）
- ✅ 添加Git双向同步规则

### 2. 文档创建 ✅
- ✅ `Cursor规则分析和整合方案.md` - 详细分析报告
- ✅ `STM32-Cursor-Rules-README.md` - 规则仓库README
- ✅ `scripts/sync-rules.sh` - 双向同步脚本

### 3. 规则更新 ✅
- ✅ 规则版本升级：v1.0.0 → v1.1.0
- ✅ 规则文件大小：741行 → 1167行（+426行）
- ✅ 新增功能：元规则、检查点、验证流程、双向同步

### 4. 本地提交 ✅
- ✅ 所有更改已提交到本地Git
- ✅ Commit: "feat: integrate cursor rules v1.1.0 with meta rules and sync system"

---

## 📋 待完成的工作

### 第1步：推送到项目仓库（double-led）✅

**作用**：备份规则到项目仓库

**执行命令**：
```powershell
cd "D:\stm32\BilibiliProject\500 double led"

# 配置代理
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897

# 推送到项目仓库
git push origin master

# 创建Tag
git tag -a 500_double_led-Rules-v1.1.0 -m "规则系统v1.1.0 - 添加元规则、检查点和同步系统"
git push origin --tags

# 清理代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

### 第2步：创建新的GitHub仓库 ⏳

**仓库信息**：
- 仓库名称：`stm32-cursor-rules`
- 可见性：Public（推荐）或 Private
- 描述：STM32 Cursor AI Development Rules Repository
- 不要初始化README（我们已有）

**创建方法**：

#### 方法A：使用GitHub网页（推荐）

1. 访问：https://github.com/new
2. 填写信息：
   - Repository name: `stm32-cursor-rules`
   - Description: `STM32 Cursor AI Development Rules Repository`
   - 选择：Public
   - ❌ 不要勾选 "Add a README file"
   - ❌ 不要勾选 "Add .gitignore"
   - ❌ 不要勾选 "Choose a license"
3. 点击 "Create repository"

#### 方法B：使用GitHub CLI（需要安装）

```powershell
gh repo create cxs00/stm32-cursor-rules --public --description "STM32 Cursor AI Development Rules Repository"
```

### 第3步：准备规则仓库内容 ⏳

**创建规则仓库目录结构**：

```powershell
cd "D:\stm32\BilibiliProject"

# 创建临时目录用于规则仓库
mkdir stm32-cursor-rules-temp
cd stm32-cursor-rules-temp

# 初始化Git
git init
git branch -M main

# 从项目中复制规则文件
Copy-Item "D:\stm32\BilibiliProject\500 double led\.cursor\rules\current\.cursorrules" -Destination ".\"
Copy-Item "D:\stm32\BilibiliProject\500 double led\.cursor\rules\current\error_solutions.md" -Destination ".\"
Copy-Item "D:\stm32\BilibiliProject\500 double led\.cursor\rules\current\hardware_constraints.md" -Destination ".\" -ErrorAction SilentlyContinue
Copy-Item "D:\stm32\BilibiliProject\500 double led\.cursor\rules\CHANGELOG.md" -Destination ".\"
Copy-Item "D:\stm32\BilibiliProject\500 double led\STM32-Cursor-Rules-README.md" -Destination "README.md"
Copy-Item "D:\stm32\BilibiliProject\500 double led\scripts\sync-rules.sh" -Destination ".\scripts\" -Force

# 创建.gitignore
@"
# macOS
.DS_Store

# Windows
Thumbs.db
Desktop.ini

# 临时文件
*.tmp
*.bak
*~

# 编辑器
.vscode/
.idea/
"@ | Out-File -FilePath ".gitignore" -Encoding utf8

# 提交
git add .
git commit -m "feat: initial commit - STM32 Cursor Rules v1.1.0"

# 创建Tag
git tag -a v1.1.0 -m "规则系统v1.1.0 - 元规则、检查点、验证流程、双向同步"
```

### 第4步：推送到规则仓库 ⏳

```powershell
# 配置代理
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897

# 添加远程仓库
git remote add origin https://github.com/cxs00/stm32-cursor-rules.git

# 推送main分支
git push -u origin main

# 推送Tag
git push origin --tags

# 清理代理
git config --global --unset http.proxy
git config --global --unset https.proxy

# 验证
Write-Host "✅ 规则仓库创建成功！" -ForegroundColor Green
Write-Host "访问：https://github.com/cxs00/stm32-cursor-rules" -ForegroundColor Cyan
```

### 第5步：验证双向同步 ⏳

**回到项目目录测试**：

```powershell
cd "D:\stm32\BilibiliProject\500 double led"

# 测试同步状态
bash scripts/sync-rules.sh status

# 测试拉取（应该显示已是最新）
bash scripts/sync-rules.sh pull

# 测试推送（如果有修改）
# 1. 修改.cursor/rules/current/.cursorrules
# 2. bash scripts/sync-rules.sh push
```

---

## 🚀 一键执行脚本

为了方便，我创建了一键执行脚本：

```powershell
# 保存为：create-rules-repo.ps1

param(
    [string]$GitHubToken = ""
)

$ErrorActionPreference = "Stop"

Write-Host "========================================" -ForegroundColor Cyan
Write-Host "   创建规则仓库和配置双向同步" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 第1步：推送到项目仓库
Write-Host "[1/5] 推送规则到项目仓库..." -ForegroundColor Yellow
cd "D:\stm32\BilibiliProject\500 double led"

git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897

git push origin master
git tag -a 500_double_led-Rules-v1.1.0 -m "规则系统v1.1.0 - 添加元规则、检查点和同步系统" -f
git push origin --tags -f

git config --global --unset http.proxy
git config --global --unset https.proxy

Write-Host "✅ 已推送到项目仓库" -ForegroundColor Green
Write-Host ""

# 第2步：创建规则仓库（需要用户手动创建）
Write-Host "[2/5] 请在GitHub创建仓库..." -ForegroundColor Yellow
Write-Host "访问：https://github.com/new" -ForegroundColor Cyan
Write-Host "仓库名称：stm32-cursor-rules" -ForegroundColor White
Write-Host "可见性：Public" -ForegroundColor White
Write-Host "❌ 不要初始化README、.gitignore或License" -ForegroundColor Red
Write-Host ""
Read-Host "创建完成后按Enter继续"

# 第3步：准备规则仓库
Write-Host "[3/5] 准备规则仓库内容..." -ForegroundColor Yellow
cd "D:\stm32\BilibiliProject"

if (Test-Path "stm32-cursor-rules-temp") {
    Remove-Item -Path "stm32-cursor-rules-temp" -Recurse -Force
}

mkdir stm32-cursor-rules-temp | Out-Null
cd stm32-cursor-rules-temp

git init
git branch -M main

# 复制文件
Copy-Item "..\500 double led\.cursor\rules\current\.cursorrules" -Destination ".\"
Copy-Item "..\500 double led\.cursor\rules\current\error_solutions.md" -Destination ".\"
Copy-Item "..\500 double led\.cursor\rules\CHANGELOG.md" -Destination ".\"
Copy-Item "..\500 double led\STM32-Cursor-Rules-README.md" -Destination "README.md"
mkdir scripts | Out-Null
Copy-Item "..\500 double led\scripts\sync-rules.sh" -Destination "scripts\"

# 创建.gitignore
@"
.DS_Store
Thumbs.db
*.tmp
*.bak
"@ | Out-File -FilePath ".gitignore" -Encoding utf8

git add .
git commit -m "feat: initial commit - STM32 Cursor Rules v1.1.0"
git tag -a v1.1.0 -m "规则系统v1.1.0"

Write-Host "✅ 规则仓库内容准备完成" -ForegroundColor Green
Write-Host ""

# 第4步：推送到规则仓库
Write-Host "[4/5] 推送到规则仓库..." -ForegroundColor Yellow

git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897

git remote add origin https://github.com/cxs00/stm32-cursor-rules.git
git push -u origin main
git push origin --tags

git config --global --unset http.proxy
git config --global --unset https.proxy

Write-Host "✅ 已推送到规则仓库" -ForegroundColor Green
Write-Host ""

# 第5步：清理临时目录
Write-Host "[5/5] 清理临时文件..." -ForegroundColor Yellow
cd ..
Remove-Item -Path "stm32-cursor-rules-temp" -Recurse -Force
Write-Host "✅ 清理完成" -ForegroundColor Green
Write-Host ""

# 完成
Write-Host "========================================" -ForegroundColor Green
Write-Host "   ✅ 规则仓库创建成功！" -ForegroundColor Green
Write-Host "========================================" -ForegroundColor Green
Write-Host ""
Write-Host "规则仓库：https://github.com/cxs00/stm32-cursor-rules" -ForegroundColor Cyan
Write-Host "项目仓库：https://github.com/cxs00/double-led" -ForegroundColor Cyan
Write-Host ""
Write-Host "双向同步已配置完成！" -ForegroundColor Green
Write-Host "使用方法：" -ForegroundColor White
Write-Host "  bash scripts/sync-rules.sh pull   # 拉取云端更新" -ForegroundColor Gray
Write-Host "  bash scripts/sync-rules.sh push   # 推送本地更新" -ForegroundColor Gray
Write-Host "  bash scripts/sync-rules.sh status # 检查同步状态" -ForegroundColor Gray
Write-Host ""
```

---

## 📊 预期结果

### 仓库结构

**stm32-cursor-rules仓库**：
```
stm32-cursor-rules/
├── .cursorrules              # 主规则文件（1167行）
├── error_solutions.md        # 错误解决方案（15个）
├── CHANGELOG.md              # 规则变更日志
├── README.md                 # 仓库说明
├── scripts/
│   └── sync-rules.sh         # 同步脚本
└── .gitignore
```

**double-led仓库**：
```
500 double led/
├── .cursor/rules/current/
│   └── .cursorrules          # 规则文件（同步）
├── scripts/
│   └── sync-rules.sh         # 同步脚本
├── [其他项目文件...]
```

### 双向同步效果

```
┌─────────────────────────────────────────────┐
│           双向同步示意图                     │
├─────────────────────────────────────────────┤
│                                             │
│   项目仓库 (double-led)                     │
│         ↕                                   │
│   .cursor/rules/current/.cursorrules        │
│         ↕                                   │
│   规则仓库 (stm32-cursor-rules)             │
│                                             │
└─────────────────────────────────────────────┘
```

---

## ✅ 验证清单

完成后请验证：

- [ ] 项目仓库已推送最新规则（double-led）
- [ ] 规则仓库已创建（stm32-cursor-rules）
- [ ] 规则仓库包含所有必要文件
- [ ] Tag已创建：v1.1.0
- [ ] 同步脚本可以执行
- [ ] 可以从规则仓库拉取
- [ ] 可以推送到规则仓库

---

## 🎯 下一步

1. **执行上述步骤** 或 **运行一键脚本**
2. **验证双向同步功能**
3. **更新项目文档**
4. **分享规则仓库给团队**

---

**创建时间**：2025-10-28  
**版本**：v1.0  
**状态**：等待执行

